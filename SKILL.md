---
name: douyin-content-packager
description: 为中文知识、技术和观点视频执行严格的五平台内容包装：要求可读字幕/文字稿文件与至少一张清晰正面自拍，生成 YouTube、B站、小红书、抖音、视频号文案及 3:4、16:9 双版封面；写入 Markdown 后展示 A/B/C 方案，用户确认才并发成图。用户要求从字幕生成全平台发布方案、人物封面或标题简介时使用；缺少必需输入也应触发并返回门禁失败，不负责剪辑和字幕制作。
---

# 五平台文案与人物封面包装

把完整视频内容转化为五个平台各自可直接发布的标题、正文和标签，并用同一设计语言制作独立构图的横竖双版人物封面。

## 硬门禁

每次运行必须同时具备：

1. 一个可读的字幕或文字稿文件；
2. 至少一张实际查看后确认清晰正面的主人公自拍照。

两到三张不同角度、表情或手势的照片是推荐输入，不能替代正面照。任一必需输入缺失或不合格时，只返回“门禁未通过”和最短补交清单，然后停止。读取 [input-handling.md](references/input-handling.md) 检查格式、文字覆盖度与照片质量；对话中粘贴的文字不能替代字幕文件。

## 脚本 Pipeline

`scripts/pipeline.py` 是状态、顺序和图片额度授权的唯一来源。开始任务后完整读取 [pipeline.md](references/pipeline.md)，只通过脚本命令推进；非零退出表示当前门禁阻塞，不能靠对话判断绕过。

```text
G0/G1 字幕与自拍通过
  → G2 全文理解
  → G3 五平台文案逐项校验并写入 Markdown
  → G4 展示 A/B/C 双比例方案
  → STOP：等待用户选择并明确确认生成
  → G5 一次预检后并发发起 3:4 与 16:9 两个独立成图请求
  → G6 双图 QA；失败图需要新的重试授权
```

## 五平台文案

读取 [packaging-playbook.md](references/packaging-playbook.md)。同一条传播命题必须按平台重新表达，默认且固定输出 YouTube、B站、小红书、抖音、视频号，不再输出“通用标题/简介”。保留三类附加内容：YouTube 时间戳、B站粉丝动态、各平台标签。

频道名和个人网站链接是可选输入：提供后自然写入对应正文/标签；未提供时完全省略相关语句和字段，成品中不得出现占位符、待补充或发布前替换提醒。输出不含发布时间、Newsletter、网站同步、数据追踪或下期选题。

通过五个平台校验后必须调用 Pipeline 写出 Markdown。默认路径为当前工作区的 `outputs/<字幕文件名>-全平台发布方案.md`；已有同名文件时创建 `-v2`、`-v3` 等新文件，不覆盖用户文件。

## 封面方案与生成

读取 [cover-playbook.md](references/cover-playbook.md)，并查看内置视觉参考。用户自拍只提供人物身份；内置参考只提供版式、配色、文字层级和信息密度。

Markdown 写入后，在对话中展示 A/B/C 三套方案。每套分别描述 3:4 与 16:9 的独立构图；两版共享人物、封面文字、主题对象、配色与设计语言，不能由一版裁切得到另一版。方案展示后停止，选择、讨论或修改不等于成图授权。

用户明确选定方案并确认生成后，遵循当前 `imagegen` Skill：

1. 通过 `approve-plan --confirm` 登记授权，再以 `can-generate-pair` 对两个比例做一次原子预检；
2. 只有预检返回 `allowed: true` 才能继续；
3. 为 3:4 与 16:9 准备两份不同构图的提示词，在同一批并发发起两个独立的 built-in 图像生成调用；不串行等待第一张完成后再请求第二张；
4. 两个调用完成后分别登记文件并检查人物身份、逐字文字、主题对象、构图、风格和缩略图可读性；
5. 任一图片失败时先交付 QA 结论，只有用户再次授权才对失败比例重试。

首次授权覆盖两张首轮图片，不覆盖任何重试。built-in 图像能力不可用时说明现状；只有用户明确选择 CLI/API fallback 时才切换，不索要或暗示粘贴 API key。

## 完成标准

- Pipeline 状态显示五个平台文案均已登记，Markdown 文件已写入；
- Markdown 包含五个平台、YouTube 时间戳、B站粉丝动态与各平台标签；
- 可选品牌字段遵循“有则写入、无则完全消失”；
- 未获生成授权时停在 A/B/C 方案；获授权后双比例首轮请求并发派发；
- 两张图片只有在 Pipeline QA 均通过时才算完整交付。
